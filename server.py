# -*- coding: UTF-8 -*-from tool.service import *if __name__ == '__main__':    HOST = '127.0.0.1'    PORT = 9999    ADDR = (HOST, PORT)    Handler =""    Ser = Server(ADDR, Handler)    #Ser.connect()    #buf = Ser.receive()    #print "Server Receive %s" % buf    #Ser.close()    while True:        try:            Ser.connect().settimeout(5)            buf = Ser.receive()            print "Server Receive %s" % buf        except socket.timeout:            print 'time out'        Ser.close()    pass